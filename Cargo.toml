[dependencies]
# Append the vendored feature to your existing OpenSSL dependency
openssl = { version = "0.10", features = ["vendored"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
